<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tienda Skincare y Cuidado Personal</title>
    
    <!-- Aquí comienza el CSS (diseño visual de la página) -->
    <style>
        /* Estilo general del cuerpo de la página */
        body {
            margin: 0; 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Fuente de texto un poco más moderna */
            display: flex; 
            height: 100vh; 
        }

        /* Estilo para la parte izquierda (Información de la tienda) */
        .izquierda {
            flex: 1; /* Ocupa la mitad del ancho total */
            background-color: #f7f1e3; /* Fondo Color Crema Claro (más de Skincare) */
            padding: 40px; 
            display: flex; 
            flex-direction: column; 
            justify-content: center; /* Centra verticalmente el contenido */
        }

        /* Estilo para la parte derecha (donde va la foto o logo) */
        .derecha {
            flex: 1; /* Ocupa la otra mitad del ancho */
            display: flex; 
            justify-content: center; 
            flex-direction: column; 
            align-items: center; 
            background-color: #d1b3ff; /* Fondo Color Morado Pastel/Suave */
        }

        /* Estilo para la imagen */
        img {
            width: 300px; 
            height: 300px; 
            border-radius: 50%; /* Hace la imagen circular */
            object-fit: cover; 
            box-shadow: 0 8px 16px rgba(0,0,0,0.3); /* Sombra un poco más fuerte */
        }

        /* Estilo para los títulos (h1) */
        h1 {
            color: #574b90; /* Morado oscuro para los títulos */
            font-size: 2.5em; /* Tamaño de fuente grande */
        }

        /* Estilo para los párrafos */
        p {
            color: #333333; /* Color gris oscuro para el texto */
            line-height: 1.8; /* Espaciado entre líneas para mejor lectura */
            font-size: 1.1em;
        }
    </style>
</head>

<body> <!-- Cuerpo del documento, donde está el contenido visible -->

    <!-- Sección izquierda: información de la tienda -->
    <div class="izquierda">
        <h1>¡Hola!! Tienda Skincare & Cuidado Personal</h1> <!-- Título principal adaptado -->
        <p>
            Somos tu mejor opción para el cuidado de la piel. Descubre nuestra selección de productos naturales y orgánicos. ¡Dale a tu piel el amor que se merece!
        </p>
        <p>
            Este código es el **Reporte 1** para la materia de Programación (OOP), demostrando el uso de HTML y CSS.
        </p>
    </div>

    <!-- Sección derecha: imagen de producto/logo -->
    <div class="derecha">
        <h1>¡Productos en Tendencia!</h1>
        <p>
            <img src="image.jpg" alt="Logo de Skincare o Producto Destacado"> <!-- Imagen (debes subir un archivo llamado 'image.jpg' si quieres que se vea) -->
        </p>
    </div>

</body>
</html>
